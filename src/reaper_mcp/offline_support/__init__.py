"""Offline RPP parsing + audio analysis support — vendored from dschuler36/reaper-mcp-server.

These modules operate on ``.RPP`` files on disk and on REAPER's plugin cache; they do
not require REAPER to be running and do not use python-reapy.
"""
